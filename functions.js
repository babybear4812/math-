export default functions = {
  sqrt: Math.sqrt,
  abs: Math.abs,
  ln: Math.log,
  log: Math.log,
  round: Math.round,
  roundDown: Math.floor,
  roundUp: Math.ceil,
  max: Math.max,
  min: Math.min,
  exp: Math.exp,
  sin: Math.sin,
  cos: Math.cos,
  tan: Math.tan,
  random: Math.random,
  arithmeticSum: (n) => {
    return (n * (n + 1)) / 2;
  },
};
